/**
 * DAĞ S.K. — Yapay Zekâ Destekli Duruş & Form Analizi (AI Pose Detection)
 * Root mirror
 */
(function (global) {
    if (typeof window !== 'undefined' && window.DAGSK_AI_POSE) return;
})(this);
